# 執行緒、解碼流程與控制架構

## 章節導覽

- [設計規則](#設計規則)
  - [執行角色與責任](#執行角色與責任)
  - [非同步交接與平行執行](#非同步交接與平行執行)
    - [非同步交接](#非同步交接)
    - [Task parallelism](#task-parallelism)
    - [管線中的套用方式](#管線中的套用方式)
  - [標頭優先的資源準備原則](#標頭優先的資源準備原則)
  - [單張圖片的處理時序](#單張圖片的處理時序)
  - [主執行緒的控制邊界](#主執行緒的控制邊界)
- [實作對應](#實作對應)
  - [關係種類](#關係種類)
  - [基礎設施](#基礎設施)
  - [狀態與資源所有權](#狀態與資源所有權)
  - [啟動流程](#啟動流程)
  - [共用流程](#共用流程)
  - [事件處理](#事件處理)
  - [任務執行器與外部事件來源](#任務執行器與外部事件來源)
  - [整體關係](#整體關係)

## 設計規則

本章定義跨函式庫、作業系統介面與圖形 API 保持穩定的設計規則。每種執行機制都必須維持相同的責任歸屬、提交邊界、完成條件與資源所有權。

本章規則不得因實作調整而擅自更動。實作需求若必須變更設計規則，應先說明變更原因與影響，並經過審查後才能修改。

### 執行角色與責任

系統由一個協調角色與三類任務執行角色組成。下表只界定每項工作的責任歸屬與交付結果；執行時必須維持的非同步與平行關係由下一節定義。

| 角色 | 唯一責任 | 輸出 |
| --- | --- | --- |
| 主執行緒 | 序列化全域控制狀態，依事件決定狀態轉移並建立工作描述或狀態授權 | 工作描述、狀態授權 |
| CPU 任務執行器 | 執行 CPU 密集的資料處理工作 | 完成結果與資源參照 |
| 儲存任務執行器 | 執行目錄與檔案系統工作 | 圖片標頭、檔案內容或圖片檔案清單 |
| 圖形任務執行器 | 執行資源傳輸、繪製與畫面提交工作 | 傳輸或畫面結果 |

新增工作時，依工作語意與狀態所有權選擇上述角色。每項資源由唯一角色負責建立、回收與重用；任務執行器只取得工作期間有效的使用授權。資源參照只用於定位資源，不參與資源生命週期；工作完成或取消後交回使用授權，資源才能回收或重用。

### 非同步交接與平行執行

#### 非同步交接

每項工作依下列生命週期跨越主執行緒與任務執行器：

- **提交**：主執行緒建立工作描述與資源使用授權，將工作交給對應執行器後結束本次控制步驟。
- **獨立執行**：提交與完成之間，主執行緒與任務執行器在各自具有可執行工作時同時推進。
- **完成**：任務執行器產生完成事件；主執行緒收到事件後接收結果、收回資源使用授權並轉移全域控制狀態。

#### Task parallelism

> 兩個以上的 task 在執行期間彼此獨立，並在同一時刻同時執行各自的工作本體。

資源授權界定 task 在執行期間可持續使用，且不與其他同時執行 task 衝突的資源。

- **派發前**：task 所需的輸入資料已就緒，並取得資源授權。
- **執行期間**：獨立推進並持續持有資源授權，不因資源搶占而分時、阻塞或等待。
- **取消**：控制狀態變更可以發出取消要求；task 停止存取後才交回資源。
- **完成時**：以完成事件同時交回結果與資源。

#### 管線中的套用方式

**非同步交接**：每張圖片依序通過儲存、CPU 與圖形階段。主執行緒提交目前階段後返回事件處理；目前階段的完成事件提供下一階段所需的輸入，輸入與資源均就緒後再派發下一階段。

**平行處理**：不同圖片分別位於儲存、CPU 與圖形階段，各階段的 task 在資源授權允許時同時執行。各任務執行器執行已提交 task 的同時，主執行緒處理已就緒事件；沒有已就緒事件時進入中央事件等待。

**執行緒數量**：應用程式執行緒固定由一個主執行緒與固定數量的 CPU Worker 執行緒組成。主執行緒負責協調；CPU Worker 執行緒只執行會隨資料量成長的 CPU 密集 task。儲存任務執行器與圖形任務執行器不建立其他應用程式執行緒。

各角色採用的具體執行資源、提交介面、資源種類與容量計算方式由實作層對應。

### 標頭優先的資源準備原則

首次接觸圖片時，儲存管線必須優先產生足以決定後續資源配置的標頭資訊。標頭資訊就緒後即可驗證格式、決定輸出尺寸與配置、保留記憶體額度並準備輸出資源；其餘檔案內容可在同一期間繼續讀取。

後續讀取同一張圖片且已保存的標頭資訊仍有效時，該資訊直接啟動資源準備，儲存任務執行器只需提供本次 CPU 工作所需的完整內容。這項原則由以下完成條件定義：

- 「標頭就緒」表示後續資源形狀已可決定。
- 「內容就緒」表示 CPU 工作所需的輸入內容已完整。
- 「資源就緒」表示輸出資源與記憶體額度已準備完成。
- 三項條件同時成立後，工作才具備進入 CPU 任務執行器的資格。

首次讀取時，標頭資訊與其餘檔案內容以非同步工作分成兩條可平行推進的邏輯路徑。標頭路徑推動格式驗證與輸出資源準備，內容路徑取得 CPU 工作所需的完整輸入。兩條路徑的完成條件最後匯合，決定工作何時可以派發：

```text
圖片需求
├─優先取得標頭資訊─▶ 標頭就緒 ─▶ 驗證與準備輸出資源 ─▶ 資源就緒
└─取得其餘檔案內容──────────────────────────────────▶ 內容就緒

標頭就緒 ＋ 內容就緒 ＋ 資源就緒 ─▶ 可派發 CPU 工作
```

儲存介面可以依平台能力選擇請求數量、資料切割、對齊方式與完成通知機制。每種實作都必須保留上述三項完成條件，並維持資源準備與其餘內容讀取的重疊關係。

### 單張圖片的處理時序

```text
主執行緒          儲存任務執行器       CPU 任務執行器       圖形任務執行器
    │                    │                    │                    │
    │─非同步提交讀取────▶│                    │                    │
    │◀─標頭就緒事件──────│                    │                    │
    │─更新資源需求並推進資源準備              │                    │
    │◀─內容就緒事件──────│                    │                    │
    │─非同步派發 CPU 工作────────────────────▶│                    │
    │◀─CPU 工作完成事件───────────────────────│                    │
    │─非同步提交資源傳輸─────────────────────────────────────────▶│
    │◀─資源傳輸完成事件───────────────────────────────────────────│
    │─建立呈現授權並非同步提交繪製───────────────────────────────▶│
    │◀─畫面完成或呈現額度事件─────────────────────────────────────│
```

圖中的垂直順序只表示同一張圖片的資料相依性；箭頭沿用前節定義的非同步交接。具體 API 與資料結構由實作層選擇。

### 主執行緒的控制邊界

主執行緒以維持低控制延遲為首要目標，及時完成狀態協調與工作交接；必須由主執行緒同步完成的操作維持在必要範圍內。

## 實作對應

前章的設計規則是責任與完成條件的依據；本章將程式元件對應到各項設計角色與交接契約。

### 關係種類

```text
事件→ 外部輸入或非同步完成通知，經事件分派器送至固定事件處理流程
讀取→ 使用狀態或資料並保持原值
寫入→ 建立或取代狀態或資料
讀寫→ 讀取後修改同一狀態或資源
擁有→ 對指定狀態、資源生命週期或共享通道承擔唯一管理責任
計算→ 產生本次同步呼叫使用的暫時選擇或順序，並保持持久狀態原值
判斷→ 依已讀取資料選擇控制分支，並保持持久狀態原值
呼叫→ 事件處理流程立即執行事件專用流程或共用流程
命令→ 呼叫固定成本的控制 API，並在同一個控制步驟內返回
派發→ 非同步將 CPU 工作描述放入工作佇列；Worker 完成後產生完成事件
提交→ 非同步將儲存要求或圖形命令交給外部執行資源；完成後產生系統事件
```

事件參數攜帶事件來源資料；其餘判斷所需的全域控制狀態與工作資源由應用程式責任邊界提供，並由主執行緒依序修改。各任務執行器自行管理其內部狀態。

### 基礎設施

```text
[基礎設施] 主事件迴圈
│
├─讀取→ Windows 訊息
├─讀取→ 目錄 I/O 完成訊號
├─讀取→ 檔案 I/O 完成訊號
├─讀取→ Worker 通知訊號
├─讀取→ GPU 上傳完成訊號
├─讀取→ DXGI Frame Latency Waitable Object
└─事件→ [基礎設施] 事件分派器
```

```text
[基礎設施] 事件分派器
│
├─事件：開啟圖片 [外部輸入]────────────→ [事件處理流程] 開啟圖片
├─事件：方向鍵步進 [Windows 輸入]──────→ [事件處理流程] 方向鍵步進
├─事件：方向鍵放開 [Windows 輸入]──────→ [事件處理流程] 方向鍵放開
├─事件：目錄 I/O 完成 [Windows 完成]────→ [事件處理流程] 目錄 I/O 完成
├─事件：PNG 標頭就緒 [Windows 完成]────→ [事件處理流程] PNG 標頭就緒
├─事件：檔案 I/O 完成 [Windows 完成]────→ [事件處理流程] 檔案 I/O 完成
├─事件：Worker 通知 [Worker 通知]────────→ [事件處理流程] Worker 通知
├─事件：GPU 上傳完成 [GPU 完成]────────→ [事件處理流程] GPU 上傳完成
├─事件：畫面提交額度可用 [DXGI 通知]──→ [事件處理流程] 畫面提交額度可用
├─事件：切換全螢幕 [Windows 輸入]──────→ [事件處理流程] 切換全螢幕
└─事件：顯示表面已改變 [Windows 通知]──→ [事件處理流程] 顯示表面已改變
```

事件分派器依事件種類執行固定處理流程；狀態讀取、需求規劃、工作派發、呈現授權及圖形工作提交由對應流程執行。

### 狀態與資源所有權

全域控制狀態、工作資源與共享通道的生命週期由主執行緒依明確順序管理；儲存、圖形與 CPU 任務執行器各自管理內部狀態。事件處理流程與共用流程存取應用程式責任邊界內的控制狀態與工作資源，並將工作期間的資源使用授權交給對應任務執行器。

```text
[應用程式責任邊界] 控制狀態與工作資源
│
├─擁有→ [狀態] 圖片索引範圍
├─擁有→ [資源] 圖片檔案清單
│
├─擁有→ [狀態] 導航授權
│         ├─按住方向
│         ├─最後有效導航方向
│         ├─待完成短按承諾
│         ├─長按暫定終點
│         └─目前呈現索引
│
├─擁有→ [狀態] 緩衝需求
│         ├─必須依序呈現範圍
│         ├─解碼預取範圍
│         └─GPU 預取範圍
│
├─擁有→ [狀態] 每張圖片的需求狀態
│         ├─範圍外
│         ├─已要求
│         └─失敗
├─擁有→ [索引] 每張圖片目前關聯的 slot ID
│         ├─壓縮 PNG slot ID
│         └─解碼 staging texture slot ID
│
├─擁有→ [slot 儲存區] 壓縮 PNG [內容寫入：Windows I/O；內容讀取：Worker]
│         └─[slot 狀態]
│             ├─可分配
│             ├─正在接收檔案資料
│             ├─可供解碼
│             ├─解碼器正在讀取
│             └─等待目前工作結束後回收
├─擁有→ [可分配索引] 壓縮 PNG slot ID
├─擁有→ [狀態] 壓縮資料記憶體使用量與預估解碼輸出保留額度
│
├─擁有→ [slot 儲存區] 解碼 staging texture [內容寫入：取得工作的單一 Worker；內容讀取：GPU]
│         ├─[暫時資源] 圖形裝置完成前的首圖 CPU 解碼記憶體
│         └─[slot 狀態]
│             ├─可分配
│             ├─已準備
│             ├─已映射且解碼器正在寫入
│             ├─已解碼且可供 GPU 複製
│             ├─GPU 正在讀取
│             └─等待解碼結束後回收
├─擁有→ [可分配索引] 解碼 staging texture slot ID
├─擁有→ [狀態] staging 記憶體使用量
├─擁有→ [共享通道] 工作佇列 [生產端：批次填充工作佇列；消費端：Worker]
├─擁有→ [共享通道] 完成佇列 [生產端：Worker；消費端：Worker 通知處理流程]
│
├─擁有→ [slot 儲存區] GPU texture [內容寫入：GPU；內容讀取：Direct2D]
│         └─[slot 狀態]
│             ├─可分配
│             ├─GPU 正在寫入
│             ├─可供 Direct2D 繪製
│             └─等待先前繪製完成
├─擁有→ [可分配索引] GPU texture slot ID
├─擁有→ [索引] 可呈現 GPU 圖片
│         └─圖片索引 → 可供 Direct2D 繪製的 GPU texture slot ID
├─擁有→ [狀態] GPU 記憶體使用量
├─擁有→ [狀態] GPU 上傳工作
│         ├─圖片索引
│         ├─解碼 staging texture slot ID
│         ├─GPU texture slot ID
│         └─完成值
│
├─擁有→ [狀態] 畫面提交額度
├─擁有→ [狀態] 視窗與全螢幕狀態
└─擁有→ [狀態] 顯示表面實體像素尺寸
```

### 啟動流程

主執行緒先建立儲存任務執行器、完成通知與固定容量的 slot 狀態儲存區，並設定執行優先權。一般圖片瀏覽工作階段接著為首圖建立暫時圖片檔案清單與資源保留，立即提交首圖讀取，再提交首圖所在目錄的非同步列舉；主執行緒隨後建立視窗與 CPU 任務執行器。

```text
主執行緒                 儲存任務執行器          CPU 任務執行器          圖形任務執行器
    │                          │                       │                       │
    ├─建立儲存任務執行器與完成通知                     │                       │
    ├─建立固定容量的 slot 狀態儲存區                    │                       │
    ├─設定主執行緒執行優先權                           │                       │
    ├─建立首圖暫時圖片檔案清單與資源保留               │                       │
    ├─提交標頭優先的首圖讀取─────────────────────────▶│                       │
    ├─提交首圖所在目錄的非同步列舉───────────────────▶│                       │
    ├─建立未顯示的視窗與 CPU 任務執行器                │                       │
    │                          │                       │                       │
    ├─接收並處理已就緒的儲存完成事件                  │                       │
    │  ├─標頭就緒：驗證格式並準備首圖解碼輸出資源     │                       │
    │  ├─內容就緒：首圖完整輸入可供解碼               │                       │
    │  └─目錄列舉完成：建立完整圖片檔案清單與資源保留 │                       │
    ├─每次處理後判斷首圖三項條件                      │                       │
    │  └─條件就緒時立即派發 PNG 解碼────────────────▶│                       │
    ├─每次處理後判斷首圖內容與圖片檔案清單            │                       │
    │  └─兩者就緒時一次提交其餘全部可執行的檔案讀取─▶│                       │
    ├─同步初始化圖形裝置      │                       │                       │
    │                          ├─推進已提交的檔案讀取   ├─執行已派發的 PNG 解碼 │
    │                          │                       │                       │
    ├─接收圖形初始化期間完成的儲存結果                │                       │
    ├─後續 PNG 解碼直接使用 staging texture─────────▶│                       │
    ├─完成其餘圖形資源初始化                                                     │
    ├─顯示視窗                 │                       │                       │
    └─進入主事件迴圈           │                       │                       │
       ├─首圖解碼成功時將 CPU 結果一次複製至 staging texture                  │
       ├─提交資源傳輸───────────────────────────────────────────────────────▶│
       └─資源傳輸完成後繪製並提交畫面───────────────────────────────────────▶│
```

首圖解碼只取決於「標頭就緒、內容就緒、資源就緒」，不等待目錄列舉完成。其餘檔案讀取則在首圖內容與完整圖片檔案清單都就緒後一次提交；主執行緒隨即同步初始化圖形裝置，使首圖解碼、其餘檔案讀取與圖形初始化平行推進。

圖形裝置初始化前派發的首圖使用暫時 CPU 解碼記憶體；裝置完成後，成功的解碼結果只複製一次至解碼 staging texture。其後圖片直接解碼至 staging texture；可供繪製的 GPU texture 在解碼完成並提交資源傳輸時建立或重用。

### 共用流程

圖中未加前綴的狀態與資源都位於應用程式責任邊界內。

#### 推進管線

```text
[共用流程] 推進管線
│
├─呼叫→ 重新計算緩衝需求
├─呼叫→ 回收範圍外工作與 slot
├─呼叫→ 批次填充工作佇列
├─呼叫→ 批次提交可執行的檔案讀取
├─呼叫→ 批次提交可執行的 GPU 上傳
├─呼叫→ 嘗試提交下一張畫面
└─判斷→ 畫面提交改變控制狀態時，依更新後的狀態重新推進
```

所有需要推進管線的事件處理流程都在完成事件專用的狀態更新後呼叫本流程。共用流程依同一順序重新計算需求、回收不再需要的工作與資源、提交已滿足條件的工作，並在畫面提交改變目前呈現位置後立即處理新的需求範圍。

#### 重新計算緩衝需求

```text
[共用流程] 重新計算緩衝需求
│
├─讀取→ 圖片索引範圍
├─讀取→ 按住方向
├─讀取→ 最後有效導航方向
├─讀取→ 待完成短按承諾
├─讀取→ 長按暫定終點
├─讀取→ 目前呈現索引
│
├─寫入→ 必須依序呈現範圍
├─寫入→ 解碼預取範圍
└─寫入→ GPU 預取範圍
```

輸入狀態、索引邊界及目前呈現位置決定導航授權與緩衝範圍；解碼結果決定圖片進入後續管線的時機。

#### 回收範圍外工作與 slot

```text
[共用流程] 回收範圍外工作與 slot
│
├─讀取→ 必須依序呈現範圍
├─讀取→ 解碼預取範圍
├─讀取→ GPU 預取範圍
├─讀寫→ 每張圖片的需求狀態與關聯 slot ID
├─讀寫→ 工作佇列的生產端與工作取消操作
├─讀寫→ 壓縮 PNG slot 狀態與可分配索引
├─讀寫→ 解碼 staging texture slot 狀態與可分配索引
├─讀寫→ GPU texture slot 狀態與可分配索引
├─讀寫→ 可呈現 GPU 圖片索引
├─讀寫→ staging 與 GPU 記憶體使用量
│
├─判斷→ 已要求且尚未取得 slot 的索引離開需求範圍
│   └─寫入→ 需求狀態為範圍外
├─判斷→ 壓縮 slot 正在接收檔案資料且索引已離開需求範圍
│   ├─寫入→ 壓縮 slot 為等待目前工作結束後回收
│   └─命令→ CancelIoEx
├─判斷→ 仍在工作佇列且索引已離開需求範圍
│   └─寫入→ 在工作佇列內取消；只有尚未被 Worker 取得時立即釋放資源
├─判斷→ Worker 已取出且索引已離開需求範圍
│   └─寫入→ 完成後可直接釋放
├─判斷→ 解碼 staging texture 位於保留範圍外且未被 Worker 或 GPU 使用
│   └─寫入→ staging slot 為可分配並加入可分配索引
└─判斷→ 可呈現 GPU 圖片位於保留範圍外且索引與目前畫面相異
    ├─寫入→ 從可呈現 GPU 圖片索引移除圖片
    └─寫入→ 對應 GPU texture slot 等待先前繪製完成後設為可分配並加入可分配索引
```

slot 狀態儲存區在整個工作階段維持固定容量、slot ID 範圍與各 slot ID 的狀態位置。這些固定項目界定可同時保留的名額與資源身分；實體資源按需求與使用授權關聯至 slot ID，並在授權交回後重用或解除。回收時解除圖片與 slot ID 的關聯並更新 slot 狀態。可分配索引專門提供可用 slot ID；工作提交、完成處理與狀態轉換都直接攜帶 slot ID，資源參照不延長實體資源的生命週期。呈現時透過可呈現 GPU 圖片索引直接取得 GPU texture slot ID。

資源槽管理直接內嵌於主狀態；三組執行期大小的動態陣列在建構時直接建立最終大小，之後不再改變容量。現行 x64 結構大小分別為壓縮槽 200 位元組、解碼暫存槽 112 位元組、GPU 紋理槽 64 位元組；合法上限組合約需 1.47 MiB，不能安全放入典型的 1 MiB Windows 執行緒堆疊。固定容量的 heap 儲存區同時確保內嵌的非同步 I/O 控制塊與完成識別在未完成期間保持有效。

#### 批次提交可執行的檔案讀取

```text
[共用流程] 批次提交可執行的檔案讀取
│
├─讀取→ 必須依序呈現範圍
├─讀取→ 解碼預取範圍
├─讀取→ 圖片檔案清單
├─讀取→ 每張圖片的需求狀態與關聯 slot ID
├─讀寫→ 壓縮 PNG slot 狀態與可分配索引
├─讀取→ 壓縮資料記憶體使用量
├─讀取→ 預估解碼輸出保留額度
├─讀取→ staging 記憶體額度
│
├─計算→ 範圍內已要求且尚未關聯任何 slot 的圖片
├─計算→ 必須呈現的相鄰圖片優先，其次才是預取圖片
├─判斷→ 對每個目前可容納的圖片
│   ├─寫入→ 取得可分配的壓縮 slot 並設為正在接收檔案資料
│   ├─寫入→ 保留預估解碼輸出額度
│   ├─寫入→ 圖片關聯壓縮 slot ID
│   ├─判斷→ 標頭資訊狀態
│   │   ├─尚未取得 ─▶ 建立「標頭就緒」與「內容就緒」兩條邏輯路徑
│   │   └─已快取 ───▶ 直接沿用標頭資訊並建立「內容就緒」路徑
│   └─提交→ 符合標頭優先原則的 Windows 非同步檔案 I/O 工作描述
└─判斷→ 待讀需求已全部提交，或下一筆會超過記憶體或 slot 數量上限時結束
```

首圖內容就緒前，本流程只提交首圖讀取。此條件解除後，每次執行本流程都一次提交全部已取得壓縮資料資源保留且目前可執行的讀取；提交集合由需求範圍、記憶體上限與壓縮 slot 數量上限共同決定。每張圖片的讀取工作描述都必須遵守 [標頭優先的資源準備原則](#標頭優先的資源準備原則)。任何釋放 slot 或記憶體額度的事件都會透過推進管線重新執行本流程。

#### 批次填充工作佇列

```text
[共用流程] 批次填充工作佇列
│
├─讀取→ 必須依序呈現範圍
├─讀取→ 解碼預取範圍
├─讀取→ 每張圖片的需求狀態與關聯 slot ID
├─讀寫→ 壓縮 PNG slot 狀態
├─讀寫→ 解碼 staging texture slot 狀態與可分配索引
├─讀寫→ 工作佇列的生產端
│
├─計算→ 壓縮資料可用且仍需要解碼的圖片
├─計算→ 必須呈現的相鄰圖片優先，其次才是預取圖片
├─判斷→ 對每個壓縮內容與解碼輸出資源都已就緒的圖片
│   ├─命令→ 準備解碼輸出資源的可寫入範圍
│   ├─寫入→ 壓縮 slot 為解碼器正在讀取
│   ├─寫入→ 圖片關聯解碼 staging texture slot ID
│   └─寫入→ 工作佇列工作
│       ├─壓縮 PNG slot ID
│       └─解碼 staging texture slot ID
└─判斷→ 可解碼圖片已全部派發或 staging texture 全部使用中時結束
```

解碼輸出資源在標頭就緒後進入已準備狀態。圖形裝置完成前，首圖的解碼輸出資源是已由標頭處理流程準備的暫時 CPU 解碼記憶體；圖形裝置完成後，尚未派發的工作改用解碼 staging texture。標頭資訊已保存的重新載入也經過相同的資源保留與狀態流程。若額度被較低優先且尚未交由 Worker 或 GPU 的資源佔滿，先回收其中優先度最低者，再派發下一張必須呈現的圖片。所有輸入、輸出與資源保留均已就緒的工作會一次派入佇列；Worker 數與名額不在啟動期節流。

#### 批次提交可執行的 GPU 上傳

```text
[共用流程] 批次提交可執行的 GPU 上傳
│
├─讀取→ 必須依序呈現範圍
├─讀取→ GPU 預取範圍
├─讀取→ 每張圖片的需求狀態與關聯 staging slot ID
├─讀取→ 可呈現 GPU 圖片索引
├─讀寫→ 解碼 staging texture slot 狀態
├─讀寫→ GPU texture slot 狀態與可分配索引
├─讀寫→ GPU 記憶體使用量
├─讀寫→ GPU 上傳工作
│
├─計算→ 關聯已解碼 staging slot，且不在可呈現 GPU 圖片索引或 GPU 上傳工作中的圖片
├─計算→ 下一張必須呈現的圖片優先，其次依距離排序
├─判斷→ 對每個目前可提交的圖片
│   ├─寫入→ 取得可分配的 GPU texture slot 並設為 GPU 正在寫入
│   ├─寫入→ GPU 上傳工作
│   │   ├─圖片索引
│   │   ├─解碼 staging texture slot ID
│   │   ├─GPU texture slot ID
│   │   └─GPU fence 完成值
│   ├─提交→ 建立 GPU texture，內容由本次資源傳輸寫入
│   ├─提交→ 從 staging texture 複製原始圖片範圍至 GPU texture
│   └─寫入→ staging slot 為 GPU 正在讀取
└─判斷→ 待上傳圖片已全部提交、GPU 記憶體或 slot 數量上限或提交容量已用完時結束
```

GPU texture 的內容由資源傳輸寫入，傳輸完成後進入可供繪製狀態。staging texture 在 fence 完成前持續作為非同步複製來源。

#### 嘗試提交下一張畫面

```text
[共用流程] 嘗試提交下一張畫面
│
├─讀取→ 畫面提交額度
├─讀取→ 目前呈現索引
├─讀取→ 待完成呈現授權
├─讀取→ 可呈現 GPU 圖片索引
├─讀取→ 索引直接取得的 GPU texture slot ID 與 slot 狀態
│
├─判斷→ 呈現授權序列為空時保持目前畫面
├─判斷→ 下一個相鄰索引仍在 GPU 上傳前階段時保持目前畫面並等待該索引
├─判斷→ 畫面提交額度處於等待狀態時等待後續額度事件
└─判斷→ 呈現授權、相鄰 GPU 圖片與畫面提交額度全部可用
    ├─提交→ Direct2D 使用該 GPU texture slot ID 高品質繪製至實體像素 back buffer
    ├─提交→ DXGI SwapChain Present
    ├─寫入→ 消耗一份畫面提交額度
    ├─寫入→ 目前呈現索引
    └─寫入→ 完成對應呈現授權
```

每份畫面提交額度最多推進一張。重新繪製同一張圖片會保持呈現授權與目前呈現索引原值。成功推進至相鄰圖片後，更新後的控制狀態交回推進管線。

### 事件處理

#### 開啟圖片事件

```text
[事件處理流程] 開啟圖片
│
├─呼叫→ [事件專用] 建立瀏覽工作階段
│   ├─讀取→ 事件攜帶的起始圖片路徑
│   ├─讀取→ 起始圖片可用的檔案大小資訊
│   ├─寫入→ 只含起始圖片的暫時圖片檔案清單與管線狀態
│   ├─寫入→ 暫時呈現索引
│   ├─寫入→ 第一張短按等級的呈現承諾
│   └─寫入→ 按住方向為停止
│
├─呼叫→ 推進管線
└─提交→ 原生非同步目錄列舉
```

一般資料夾工作階段先初始化首圖狀態並立刻提交首圖讀取；完整圖片檔案清單由原生非同步目錄列舉取得。驗證清單模式仍同步建立固定清單。後續呼叫依圖片需求、關聯 slot ID 與各 slot 狀態決定可立即推進的階段。

非同步目錄列舉包含 64 KiB 查詢緩衝區與核心持有的 I/O 狀態位址，因此以單次 heap 配置保存，避免擴大主執行緒堆疊，並確保未完成查詢期間的位址保持穩定。

#### 目錄 I/O 完成事件

```text
[事件處理流程] 目錄 I/O 完成
│
├─讀取→ 原生非同步目錄列舉的完成資料
├─寫入→ 解析每個 PNG 檔案的位置與大小
├─判斷→ 尚有項目時提交下一批原生非同步目錄查詢
├─寫入→ 完成時排序圖片檔案清單並定位首圖正式索引
├─寫入→ 將首圖既有 I/O 與 slot 關聯移至正式索引
├─寫入→ 初始化其餘圖片的管線狀態與導航範圍
├─寫入→ 更新尚在佇列中的首圖工作與 slot 內正式圖片索引
└─呼叫→ 推進管線
```

此處理流程只在 Windows I/O 完成事件到達後執行，並由事件迴圈統一等待通知。首圖 I/O 完成不必等待完整圖片檔案清單；合併時沿用首圖已配置的壓縮 slot、真實檔案大小及可能已排入佇列的工作，並以 slot 保存的正式圖片索引接收 Worker 結果。

#### 方向鍵步進事件

```text
[事件處理流程] 方向鍵步進
│
├─呼叫→ [事件專用] 更新導航授權
│   ├─讀取→ 方向鍵方向
│   ├─讀取→ 是否為 Windows 重複輸入
│   ├─讀取→ 圖片索引範圍
│   ├─讀寫→ 按住方向
│   ├─判斷→ 非重複輸入
│   │   └─寫入→ 增加一張必須完成的短按承諾
│   ├─判斷→ 重複輸入
│   │   └─寫入→ 長按暫定終點沿輸入方向移動一張
│   └─判斷→ 每個事件最多增加一張，並將索引限制於有效邊界內
│
└─呼叫→ 推進管線
```

輸入事件決定導航授權；解碼與 GPU 結果決定授權進入可呈現狀態的時機。

#### 方向鍵放開事件

```text
[事件處理流程] 方向鍵放開
│
├─呼叫→ [事件專用] 結束長按授權
│   ├─讀取→ 放開的方向鍵
│   ├─讀取→ 目前呈現索引
│   ├─讀寫→ 按住方向
│   ├─讀寫→ 長按暫定終點
│   ├─寫入→ 捨棄尚未呈現的長按授權
│   └─寫入→ 保留待完成短按承諾
│
└─呼叫→ 推進管線
```

#### PNG 標頭與檔案 I/O 完成事件

```text
[事件處理流程] PNG 標頭就緒
│
├─讀取→ 壓縮 PNG slot 的前 24 位元組
├─判斷→ PNG signature 與 IHDR 有效
├─寫入→ 圖片寬度、高度、解碼輸出大小與標頭就緒狀態
├─讀取→ 圖片的解碼輸出資源保留與固定 staging 記憶體額度
├─寫入→ 取得解碼 staging texture slot ID 並關聯圖片
├─判斷→ 圖形裝置已完成初始化
│   └─命令→ 建立或重用未映射的 `D3D11_USAGE_STAGING` texture
├─判斷→ 位於圖形裝置初始化前
│   └─寫入→ 準備首圖暫時 CPU 解碼記憶體
└─寫入→ 解碼輸出資源為已準備
```

解碼輸出資源的配置只取決於標頭提供的尺寸與資源保留，與其餘檔案內容是否完成無關。圖形裝置初始化期間，首圖使用標頭處理流程準備的暫時 CPU 解碼記憶體；圖形裝置完成後，該次結果複製至解碼 staging texture，後續圖片直接使用 staging texture。可供繪製的 GPU texture 在解碼完成並提交資源傳輸時建立或重用。

```text
[事件處理流程] 檔案 I/O 完成
│
├─呼叫→ [事件專用] 處理檔案 I/O 完成結果
│   ├─讀取→ 本次事件提供的 I/O 完成結果
│   ├─讀寫→ 每張圖片的需求狀態與關聯壓縮 slot ID
│   ├─讀寫→ 壓縮 PNG slot 狀態與可分配索引
│   ├─判斷→ 成功且仍需要
│   │   └─寫入→ 壓縮 slot 為可供解碼
│   ├─判斷→ 成功且索引已離開需求範圍
│   │   └─寫入→ 壓縮 slot 為可分配、加入可分配索引，並釋放預估輸出保留額度
│   └─判斷→ 失敗或取消完成
│       └─寫入→ 壓縮 slot 為可分配、加入可分配索引，並釋放預估輸出保留額度
│
└─呼叫→ 推進管線
```

成功且保留的壓縮資料持續計入記憶體用量。本次處理釋放額度時，既有等待需求會依新的可用額度進入批次提交判斷。

#### Worker 通知事件

```text
[事件處理流程] Worker 通知
│
├─呼叫→ [事件專用] 排空完成佇列中的輸入釋放通知與已就緒結果
│   ├─判斷→ 對每個輸入釋放通知
│   │   ├─寫入→ 解除圖片與壓縮 PNG slot ID 的關聯
│   │   └─寫入→ 壓縮 slot 為可分配、加入可分配索引並釋放壓縮資料
│   └─判斷→ 對每個 Worker 工作完成結果
│       ├─讀寫→ 每張圖片的需求狀態與關聯 slot ID
│       ├─讀寫→ 解碼 staging texture slot 狀態與可分配索引
│       ├─判斷→ 解碼輸出資源為 staging texture
│       │   └─命令→ 解除 staging texture 映射
│       ├─判斷→ 成功且仍需要
│       │   └─寫入→ staging slot 為已解碼且可供 GPU 複製
│       ├─判斷→ 成功且索引已離開需求範圍
│       │   └─寫入→ staging slot 為可分配並加入可分配索引
│       └─判斷→ 失敗
│           └─寫入→ 需求狀態為失敗並釋放 staging slot
│
└─呼叫→ 推進管線
```

Worker 在壓縮位元流已完全消耗後即可發出輸入釋放通知；此時僅可能剩餘不再存取壓縮資料的輸出配置整理。主執行緒收到通知後回收對應壓縮 slot。工作完成結果到達時，若解碼輸出資源為 staging texture，主執行緒先解除映射，再依結果與資源使用授權轉換 staging slot。首圖 CPU 解碼記憶體在此流程中只標記為已解碼並保留於 slot，直到 GPU 上傳提交階段才完成至 staging texture 的唯一一次交接。更新後的資源狀態由推進管線統一處理。

#### GPU 上傳完成事件

```text
[事件處理流程] GPU 上傳完成
│
├─呼叫→ [事件專用] 處理 GPU 上傳完成結果
│   ├─讀取→ 本次事件提供的 GPU 上傳完成結果
│   ├─讀寫→ GPU 上傳工作
│   ├─讀寫→ GPU texture slot 狀態與可分配索引
│   ├─讀寫→ 解碼 staging texture slot 狀態與可分配索引
│   ├─讀寫→ 可呈現 GPU 圖片索引
│   ├─判斷→ 成功且圖片仍在 GPU 保留範圍
│   │   ├─寫入→ GPU texture slot 為可供 Direct2D 繪製
│   │   ├─寫入→ 可呈現 GPU 圖片索引
│   │   │   └─圖片索引 → GPU texture slot ID
│   │   └─寫入→ staging slot 為可分配並加入可分配索引
│   ├─判斷→ 成功但圖片已離開 GPU 保留範圍
│   │   ├─寫入→ GPU texture slot 等待先前繪製完成後設為可分配並加入可分配索引
│   │   └─寫入→ staging slot 為可分配並加入可分配索引
│   ├─判斷→ 失敗
│   │   ├─寫入→ GPU texture slot 等待先前繪製完成後設為可分配並加入可分配索引
│   │   └─寫入→ staging slot 為可分配並加入可分配索引
│   └─判斷→ 每個完成結果
│       └─寫入→ 移除對應 GPU 上傳工作
│
└─呼叫→ 推進管線
```

GPU 完成會釋放複製來源 staging slot，讓等待解碼或等待讀取的既有需求重新進入執行判斷；導航需求由輸入事件建立。

#### 畫面提交額度可用事件

```text
[事件處理流程] 畫面提交額度可用
│
├─寫入→ 畫面提交額度可用
└─呼叫→ 推進管線
```

呈現流程依相鄰索引順序等待下一張進入 GPU 可用狀態，期間保留導航授權。

#### 切換全螢幕事件

```text
[事件處理流程] 切換全螢幕
│
└─呼叫→ [事件專用] 切換視窗模式
    ├─讀取→ 視窗與全螢幕狀態
    ├─寫入→ 視窗與全螢幕狀態
    ├─寫入→ 進入全螢幕前的還原資訊
    └─命令→ Windows 視窗系統套用樣式與位置
```

實際顯示表面尺寸以 Windows 後續回報的顯示表面變更事件為準。

#### 顯示表面已改變事件

```text
[事件處理流程] 顯示表面已改變
│
├─呼叫→ [事件專用] 重建顯示表面
│   ├─讀取→ Windows 回報的實體像素尺寸
│   ├─寫入→ 顯示表面實體像素尺寸
│   ├─命令→ DXGI SwapChain ResizeBuffers
│   ├─命令→ 重建 Direct2D back-buffer target
│   ├─寫入→ 目前圖片需要重新繪製
│   └─寫入→ 畫面提交額度可用
│
└─呼叫→ 推進管線
```

重新繪製目前圖片會保持目前呈現索引、呈現授權及圖片讀取需求原值。

### 任務執行器與外部事件來源

```text
[應用程式任務執行器] Worker
│
├─讀寫→ 工作佇列的消費端
├─讀取→ 工作描述指定的輸入資源 slot ID
├─計算→ 工作描述指定的 CPU 密集階段
├─寫入→ 工作描述指定的輸出資源 slot ID
├─讀寫→ 完成佇列的生產端
├─事件→ 壓縮輸入釋放 [主事件迴圈]
└─事件→ Worker 工作完成 [主事件迴圈]
```

工作佇列只包含 PNG 解碼：輸入為 Windows 非同步檔案 I/O 填入的壓縮 PNG slot，輸出為工作描述指定的解碼輸出 slot。圖形裝置完成前，首圖輸出由暫時 CPU 解碼記憶體承載；其後輸出由已映射的 staging texture 承載。所有 Worker 從同一條工作佇列領取這些 CPU 密集工作。

8 位元 RGBA、非交錯 PNG 的快速路徑在同一個 Worker、同一條同步呼叫鏈內依序執行 DEFLATE 與 PNG 篩選還原。DEFLATE 直接寫入既有輸出 slot，不配置另一份完整的篩選資料緩衝區。解壓器在既有 DEFLATE 區塊邊界將單調遞增的安全前綴交給篩選還原流程；完成處理後才繼續解壓，因此不涉及背景工作或非同步事件，也不在符號解碼快速迴圈中增加水位判斷分支。

串流完成前只處理已離開 DEFLATE 32,768 位元組歷史視窗的完整掃描列，使篩選還原能原地壓縮資料，而不改寫後續相符序列仍可能引用的位元組。每批至少 32 KiB，且不小於一個掃描列。一般圖片以 256 個掃描列、最多 8 MiB 作為共享快取區域性的批次目標；掃描列達 16 KiB 以上時，則放寬至 1,024 個掃描列、最多 32 MiB，以減少大型 8K PNG 在區塊邊界暫停解壓的次數，同時保留 Paeth 4／8 列波前處理。

DEFLATE 完整驗證成功後，再以相同的同步方式處理尾端掃描列。篩選還原耗時包含在解壓階段內；驗收分別記錄解壓進行期間已完成的資料，以及串流驗證後才完成的尾端資料。

```text
[外部非同步執行器] Windows 非同步檔案 I/O
│
├─讀取→ 批次提交流程建立的檔案範圍、長度與目的緩衝區
├─寫入→ 壓縮 PNG slot 中對應的檔案內容範圍
├─事件→ 首次讀取的標頭資訊就緒 [主事件迴圈]
└─事件→ 完整內容就緒 [主事件迴圈]
```

Windows 儲存介面的對應方式如下：

| 情境 | 機制 |
| --- | --- |
| 目錄列舉 | `NtQueryDirectoryFileEx` 原生非同步查詢 |
| 圖片檔案 | 每個以 `FILE_FLAG_OVERLAPPED` 與 `FILE_FLAG_NO_BUFFERING` 開啟的控制代碼關聯至共用 IOCP |
| 首次讀取 | 以兩筆 `ReadFile` 同時提交標頭與未被標頭涵蓋的內容範圍 |
| 標頭資訊已保存 | 沿用既有標頭資訊，並以一筆 `ReadFile` 提交完整內容 |

首次讀取的標頭範圍至少涵蓋 PNG 解析器判斷輸出資源所需的 24 位元組，並依底層儲存介面的 I/O 最小單位向上對齊。每張圖片固定保留一個內容讀取狀態；標頭範圍未覆蓋的內容由這個讀取一次提交，標頭範圍已覆蓋完整檔案時則由標頭完成同時滿足內容就緒。首圖與後續圖片都依實體對齊粒度擴張傳輸緩衝區，且不改變壓縮資料資源保留數量。標頭與內容的檔案範圍完整覆蓋本次內容且彼此不重疊。這些數值與 API 屬於 Windows 與 PNG 配接層；設計層的要求是「標頭就緒、內容就緒、資源就緒」三項完成條件及其重疊關係。

每個壓縮 PNG slot 內嵌一個位址穩定的 I/O 請求，包含標頭與內容各自的 `OVERLAPPED`。檔案控制代碼關聯至共用完成埠時，以該請求位址作為完成鍵；同步成功的要求不產生 IOCP 完成封包，檔案物件本身也不發出完成訊號，但 `OVERLAPPED` 明確指定的事件仍會發出訊號。`ReadFile` 同步成功時，主執行緒立即處理結果；只有尚未完成的讀取才由 IOCP 通知。所有尚未完成的讀取共用一個手動重設的核心事件，主事件迴圈透過 `MsgWaitForMultipleObjectsEx` 等待。事件觸發後，主執行緒先重設事件，再以零逾時的 `GetQueuedCompletionStatusEx` 批次排空完成埠；重設先於排空可避免完成封包在兩者之間抵達時遺失喚醒。整個流程不建立額外應用程式執行緒，也不另設 C++ 同步佇列、互斥鎖或原子變數。主執行緒由同步呼叫結果或完成封包取得結果與傳輸長度，並以 `OVERLAPPED` 位址分辨標頭或內容。尚未完成的讀取所使用的檔案控制代碼、壓縮緩衝區與 slot 都維持有效，直到對應完成封包取出。關閉時先以 `CancelIoEx` 取消所有待完成讀取，由主執行緒以阻塞式 `GetQueuedCompletionStatus` 排空其完成封包，再關閉檔案、核心事件與完成埠。

驗證報告中的首圖內容完成觀測時間，是主執行緒處理內容讀取完成結果的時間：同步成功時記錄直接處理結果的時間，非同步完成時記錄從 IOCP 取出完成封包的時間。IOCP 完成封包不含核心完成時間戳，因此非同步完成的觀測時間可能晚於儲存工作在核心完成的時間。

```text
[外部非同步執行器] GPU 命令佇列
│
├─讀取→ GPU 上傳工作指定的解碼 staging texture slot
├─寫入→ GPU 正在寫入的 GPU texture slot
└─事件→ GPU 上傳完成 [主事件迴圈]
```

```text
[外部事件來源] DXGI SwapChain
│
└─事件→ 畫面提交額度可用 [主事件迴圈]
```

工作佇列與完成佇列是主執行緒和 Worker 之間的共享通道。尚未由 Worker 取走的工作在工作佇列互斥鎖內取消；已取走的工作保有 slot 所有權直到完成，主執行緒只把對應資源保留標記為等待回收，收到完成結果後再保留或回收。因此不需要額外的原子取消旗標。完成佇列由多個 Worker 寫入，主執行緒依完成事件讀取結果。工作只攜帶圖片身分、世代與輸入及輸出 slot ID；slot 狀態儲存區維持固定的名額、slot ID 範圍與狀態位置，slot 狀態授權單一 Worker 在 CPU 密集工作期間讀寫指定資源。Worker 在 D3D 裝置建立前完成 CPU 解碼時，主執行緒只把 slot 標記為已解碼；CPU 解碼記憶體會留在 slot 中，直到裝置就緒後才由上傳階段轉成 D3D staging texture，避免以額外同步換取啟動順序。

每個解碼執行緒的統計資料各自對齊至 64 位元組快取列，並只依實際執行緒數建立。若直接內嵌最大 256 筆，主執行緒入口的堆疊框架會固定增加約 16 KiB，MSVC 因此會產生 C6262 警告；所以這個執行期大小的動態陣列保留 heap 配置，建構後不再改變容量。

### 整體關係

```text
[基礎設施] 主事件迴圈
│
├─讀取→ Windows 輸入與視窗通知
├─讀取→ Windows I/O、Worker、GPU 與 DXGI 完成訊號
└─事件→ 事件分派器

[基礎設施] 事件分派器
│
└─事件→ 與事件種類固定對應的單一事件處理流程

[主執行緒控制層] 事件處理流程
│
├─讀寫→ [應用程式責任邊界] 控制狀態與工作資源
├─呼叫→ 事件專用控制流程
├─呼叫→ 控制層共用流程
├─命令→ 固定成本的 Windows 視窗控制 API
├─提交→ Direct2D／DXGI 繪製與畫面提交工作
└─提交→ Windows 非同步檔案 I/O 或 GPU 資源傳輸

[主執行緒控制層] 共用流程
│
├─讀寫→ [應用程式責任邊界] 控制狀態與工作資源
├─命令→ 固定成本的視窗控制呼叫
├─提交→ Direct2D／DXGI 繪製與畫面提交工作
└─提交→ Windows 非同步檔案 I/O 或 GPU 資源傳輸

[應用程式責任邊界] 控制狀態與工作資源
│
├─擁有→ 壓縮 PNG、解碼輸出與 GPU texture 的 slot 狀態儲存區
├─擁有→ 各類可分配索引與 slot 狀態
├─擁有→ 可呈現 GPU 圖片索引與 GPU 上傳工作
├─擁有→ 工作佇列 [共享：Worker]
└─擁有→ 完成佇列 [共享：Worker]

[任務執行器與外部事件來源] Worker、Windows I/O、GPU 與 DXGI
│
└─事件→ 主事件迴圈
```
