# PhotoViewer 需求

## 1. 產品目標

- 建立原生 Windows 相片瀏覽器。
- 以每秒 10 張為目標切換 8K 圖片。
- 第一階段優先支援 PNG。
- 初期不加入非必要的複雜功能。

## 2. 使用方式

- 從使用者指定的圖片開始瀏覽同一資料夾中的圖片。
- 使用左方向鍵切換上一張，右方向鍵切換下一張。
- 單次按鍵只切換一張。
- 壓住方向鍵時依序切換，不得跳過中間圖片。
- 每個作業系統方向鍵 repeat 最多只可授權前進一張，不得在沒有新輸入時自行連續前進。
- 長按切換不限制固定速率。
- 解碼慢於 repeat 時仍須依序呈現；放開方向鍵後清除尚未呈現的 repeat，立即停在當前顯示的圖片。
- 到達資料夾邊界時停止，不循環。
- 使用 F11 切換無邊框全螢幕，再次按下 F11 還原原視窗狀態。

## 3. 圖片呈現品質

- 使用 Direct2D 高品質縮放。
- 使用 Per-Monitor DPI，並使 back buffer 對應視窗的實體像素，避免 DPI 虛擬化造成 DWM 二次縮放。
- 以實際畫面驗收清晰度與鋸齒，不能以效能測試代替畫質驗收。
